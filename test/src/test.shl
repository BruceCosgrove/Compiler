
barbar: let = 50;
barfoo: let = 100;
foobar: let = 150;
foofoo: let = 200;

main: (status: let; argc: let, argv: let) = {
    barbar = foofoo;
    status = barbar;
}

bar: (ret: let; condition: let) = {
    if condition {
        ret = 64;
        return;
    }
    ret = 42;
}

foo: (x: let, y: let; a: let, b: let, c: let) = {
    z: let = 6;
    x = a / z + b;
    y = b - c * z;
}
