
barbar := 50;
barfoo := 100;
foobar := 150;
foofoo := 200;

foo: (x: let, y: let; a: let, b: let, c: let) = {
    z := 6;
    x = a / z + b;
    y = b - c * z;
}

main: (status: let; argc: let, argv: let) = {
    //foo, bar := foo(1, 2, 3);
    barbar = foofoo;
    status = barbar;
}
