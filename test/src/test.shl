
main: (status: let; argc: let, argv: let) = {
    if status {
        status = 64;
    }
    status = 3;
}


foo: (x: let, y: let; a: let, b: let, c: let) = {
    z: let = 6;
    x = a / z + b;
    y = b - c * z;
}
