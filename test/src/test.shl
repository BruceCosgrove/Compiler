
main: (status: let; argc: let, argv: let) = {
    a: let = 5;
    b: let = 2;
    c: let = 7;
    z: let = 6;
    status = (a / z + b) + (b - c * z);
}


foo: (x: let, y: let; a: let, b: let, c: let) = {
    z: let = 6;
    x = a / z + b;
    y = b - c * z;
}
