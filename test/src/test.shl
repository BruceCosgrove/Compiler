
hmm: let = 46;

main: (status: let; argc: let, argv: let) = {
    a: let = 5;
    b: let = 2;
    c: let = 7;
    z: let = 6;
    status = hmm;
}


foo: (x: let, y: let; a: let, b: let, c: let) = {
    z: let = 6;
    x = a / z + b;
    y = b - c * z;
}
